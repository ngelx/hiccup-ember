import Ember from 'ember';

export default Ember.Route.extend({
/*  model: {
    return {'id': params.trip_id, 'password': ''};
  },
  */
  /*
  password: null,
  trip: null,
  loginFailed: false,

  actions: {
    tryPassword(){
      var that = this;
      console.log("yeay ... password:", that.controller.get('password') );
      this.toggleProperty('loginFailed');
      that.set('trip', "soemthing");
    }
  }
  */
});
